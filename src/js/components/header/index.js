const header = (() => {
    const DOM = {
        header: ""
    };

    const init =  (config = {}) => {

        console.log("header component initialization 5");
    }

    return {
        init: init
    };
})();

export default header;


